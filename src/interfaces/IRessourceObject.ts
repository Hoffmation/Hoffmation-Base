/**
 * Interface to allow for translation of strings to the different languages thus, allowing the ressources to be outsourced to a json file.
 */
export interface iRessourceObject {
  /**
   * German translation
   */
  de?: string;
  /**
   * English translation
   */
  en: string;
}
