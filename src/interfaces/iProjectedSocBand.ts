/** A projected state of charge at both edges of a band, in percent. */
export interface iProjectedSocBand {
  /** The lower edge in percent */
  lower: number;
  /** The upper edge in percent */
  upper: number;
}
