/**
 * The settings for the Müll-Service (if needed).
 * Müll-Service is a service to remind you of the different waste collections.
 */
export interface iMuellSettings {
  /**
   * URL to the calendar to fetch the events from
   */
  calendarURL: string;
}
